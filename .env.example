# Jellyfin Configuration
# JELLYFIN_URL=http://localhost:8096
# JELLYFIN_API_KEY=your-jellyfin-api-key
# JELLYFIN_USERNAME=your-jellyfin-username

# Plex Configuration (optional)
# PLEX_URL=http://localhost:32400
# PLEX_TOKEN=your-plex-token

# StreamyStats Configuration (optional — enables watchlist shuffle)
# STREAMYSTATS_URL=http://localhost:3000
# STREAMYSTATS_TOKEN=your-jellyfin-user-access-token

# Optional request timeout in seconds for upstream Jellyfin/Plex/StreamyStats calls
# REQUEST_TIMEOUT=15

# Port the API listens on (Docker image only)
# PORT=8005

# Directory holding the built frontend. Defaults to ./static, which is where the
# Docker image puts it. Leave unset when running the Vite dev server.
# STATIC_DIR=/app/static

# Comma-separated browser origins allowed to call the API. Only needed when the
# UI is served from a different origin than the API, as in the dev setup.
# CORS_ORIGINS=http://localhost:8080,http://localhost:8081
